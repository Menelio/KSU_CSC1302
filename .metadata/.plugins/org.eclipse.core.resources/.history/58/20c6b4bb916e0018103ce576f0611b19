package mod2;

import java.sql.Date;

public class test {

	public static void main(String[] args) {
		
		Time time = new Time();
		TimeVideoTut time2 = new TimeVideoTut();
		System.out.println(time.hour+":"+time.getMinute()+":"+time.second);
		
		long t =System.currentTimeMillis(); 
		
		System.out.println(       (int)((((t/(1000 * (60 * 60)))-5)%24)%12)      );
		System.out.println(         (((t/(1000 * (60 * 60)))-5)%24)%12       );
		System.out.println(time2.getHour());
		
		 
	}

}
